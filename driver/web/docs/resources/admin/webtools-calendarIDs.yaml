get:
  tags:
  - Admin
  summary: Get all webtools calendar ids
  description: |
    Get all webtools calendar ids
  security:
    - bearerAuth: []
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: "../../schemas/application/WebtoolsCalendarIDs.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error    